<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:MyCanvas="clr-namespace:CounterStrike.UIElements">



    <Style x:Key="GameCanvasStyle" TargetType="{x:Type MyCanvas:GameCanvas}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type MyCanvas:GameCanvas}">
                    <Canvas>
                        <TextBlock Canvas.Top="50" Canvas.Left="{Binding Path=OX, RelativeSource={RelativeSource TemplatedParent}}">
                            asdasdasd
                        </TextBlock>
                    </Canvas>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    
    
</ResourceDictionary>