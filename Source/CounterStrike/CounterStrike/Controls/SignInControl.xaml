<UserControl x:Class="CounterStrike.Controls.SignInControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:classicTheme="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Classic"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Background="Transparent">
    
    <UserControl.Resources>

        <Style x:Key="placeHolder" TargetType="{x:Type TextBox}" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Grid>
                            <TextBox Text="{Binding Path=Text,
                                                RelativeSource={RelativeSource TemplatedParent}, 
                                                Mode=TwoWay,
                                                UpdateSourceTrigger=PropertyChanged}"
                                 x:Name="textSource" 
                                 Background="Transparent" 
                                 Foreground="#BC956F"
                                 Panel.ZIndex="2" />
                            <TextBox Text="{TemplateBinding Tag}" Background="{TemplateBinding Background}" Panel.ZIndex="1">
                                <TextBox.Style>
                                    <Style TargetType="{x:Type TextBox}">
                                        <Setter Property="Foreground" Value="Transparent"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=Text, Source={x:Reference textSource}}" Value="">
                                                <Setter Property="Foreground" Value="LightGray"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ButtonFocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" SnapsToDevicePixels="true" 
                                       Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" 
                                       StrokeThickness="1" StrokeDashArray="1 2" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MetroButton" TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource ButtonFocusVisual}"/>
            <Setter Property="Background" Value="#EEEEEEEE"/>
            <Setter Property="Foreground" Value="#BC956F"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10 5"/>
            <Setter Property="FontSize" Value="14" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Grid>
                            <Border
                                x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" />

                            <ContentPresenter
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                Margin="{TemplateBinding Padding}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                RecognizesAccessKey="True" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="OpacityMask" Value="#D8888888"/>
                                <Setter Property="Margin" Value="2 1" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderThickness" Value="0"/>
                                <!--<Setter Property="Background" Value="DimGray"/>-->
                                <Setter Property="Foreground" Value="#0E5A8D"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="#FF71C5FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border BorderBrush="Gray" BorderThickness="2" Opacity="0.9" CornerRadius="10">
        <Grid Background="Transparent" Height="206" Width="294">
            <Grid.RowDefinitions>
                <RowDefinition Height="48*"/>
                <RowDefinition Height="51*"/>
                <RowDefinition Height="58*"/>
                <RowDefinition Height="54*"/>
            </Grid.RowDefinitions>

            <!--Zero row-->
            <TextBlock HorizontalAlignment="Left" Margin="124,10,0,0" TextWrapping="Wrap" Grid.Row="0"
                   Text="Sign In" VerticalAlignment="Top" Foreground="#0E5A8D" 
                   FontSize="22" FontWeight="Medium" Height="37" Width="82"/>

            <!--First row-->
            <TextBox HorizontalAlignment="Left" Margin="99,18,0,0" TextWrapping="Wrap" Grid.Row="1"
                 VerticalAlignment="Top" Style="{StaticResource placeHolder}" Tag="Name"
                 Foreground="#BC956F" FontSize="16" FontWeight="Medium" Width="129" Height="23"/>

            <!--Second row-->
            <ComboBox HorizontalAlignment="Left" Margin="99,10,0,0" Grid.Row="2"
                 VerticalAlignment="Top" 
                 Foreground="#BC956F" FontSize="14" FontWeight="Medium" Width="129" Height="25">
                <ComboBoxItem>Blue forces</ComboBoxItem>
                <ComboBoxItem>Green forces</ComboBoxItem>
                <ComboBoxItem>Red forces</ComboBoxItem>
            </ComboBox>

            <!--Last row-->
            <StackPanel Orientation="Horizontal" Grid.Row="3">
                <Button Content="Ok" HorizontalAlignment="Left" Margin="78,5,0,0"
        		VerticalAlignment="Top" Width="75" FontWeight="Medium"
                    Style="{DynamicResource MetroButton}"/>

                <Button Content="Cancel" HorizontalAlignment="Left" Margin="10,5,0,0"
        		VerticalAlignment="Top" Width="75" FontWeight="Medium"
                    Style="{DynamicResource MetroButton}"/>
            </StackPanel>

            <!--Images row-->
            <Image HorizontalAlignment="Left" Height="75" Margin="240,-22,-32,0" 
                   VerticalAlignment="Top" Width="86" 
                   Source="/CounterStrike;component/Media/logo.png" Grid.RowSpan="2"/>

            <Image HorizontalAlignment="Left" Height="100" Margin="-30,-32,0,0" 
                   VerticalAlignment="Top" Width="100" 
                   Source="/CounterStrike;component/Media/counterlogo.png" Grid.RowSpan="2"/>

        </Grid>
    </Border>
</UserControl>
